[pytest]
minversion = 6.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -v --tb=short
markers =
    integration: integration tests using pyISSM workflows
    slow: slow tests requiring ISSM backend execution
    unit: fast unit tests that don't require ISSM backend